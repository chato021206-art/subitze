# ULTIMATE改修スプリント 評価結果

**判定:** 合格 (PASS)
**評価日:** 2026-04-12
**評価対象:** ULTIMATE敵リネーム + マハカラ三つ目化 + UI/タイマー修正
**コミット:** c8b339d

## スコア

| 基準 | スコア | 閾値 | 判定 |
|------|--------|------|------|
| 機能完全性 | 5/5 | 4 | PASS |
| 動作安定性 | 5/5 | 4 | PASS |
| UI/UX品質 | 4/5 | 3 | PASS |
| エラーハンドリング | 4/5 | 3 | PASS |
| 回帰なし | 5/5 | 5 | PASS |

## 変更スコープ

1. ULTIMATE敵名4体リネーム
   - ジュピラン → ジュリオン
   - ブラックホウル → マハカラ
   - ギャラクシオン → アルニア
   - クリスタラー → プネーマ
2. マハカラカード再生成（三つ目+胸のもふもふ）
3. マハカラ+プロンプト刷新（解脱したマハカラ）
4. ULTIMATE背景明度向上（map/game/uSky グラデ）
5. ステージランドマーク 54→80px に拡大
6. getTimeLimit修正（s.t フォールバック追加）

## 検証結果（E1-E7）

### E1. 参照整合性 — PASS
- 旧敵名4種すべて stage.html / index.html から完全除去
- 新敵名4種すべて stage.html に登録済
- getTimeLimit は `s.t || j.gd || j.g || j.e` の安全なフォールバック順
- 呼び出し箇所3件すべて引数互換

### E2. データ構造整合性 — PASS
- STAGES.EASY = 10, NORMAL = 10, HARD = 10, EXTREME = 10, ULTIMATE = 11
- 全57ステージに `t:` フィールド存在（タイマーfix の前提が成立）
- CARD_IMAGES 92件すべてファイル存在
- ENEMIES.ULTIMATE = 11体（STAGES と一致）

### E3. 判定ロジック整合性 — PASS
- `stats.good` 参照なし
- `j.gd` 残存2件はいずれも安全：
  - L3116: ステージ情報表で `(j.gd && j.g)` 短絡評価により `'-'` を返す（j.gd は全ステージで undefined）
  - L3934: getTimeLimit のフォールバックチェーン

### E4. UI整合性 — PASS
- `.stage-landmark svg { width: 80px }` 設定済
- ULTIMATE map/game 背景 #2A2168/#241858 適用
- uSky 放射グラデーション #3A2880/#1E1450/#0C0828 で明度向上

### E5. ランタイムエラー — PASS
- 構文エラーなし（手動レビュー）

### E6. 後方互換性 — PASS
- starScore のフォールバック維持
- 旧 prog データ読込時に新フィールド欠落しても動作

### E7. パフォーマンス — PASS
- サムネイル参照は維持
- カード画像 2MB超 3件あり（許容範囲、フルサイズ表示時のみ使用）

## バグ一覧

なし

## 改善提案

1. **j.gd 残存箇所のクリーンアップ（任意）**
   - L3116 の `gdWin` カラムは GOOD判定削除後は常に `'-'` を表示するため、完全に削除可能。
   - 現状実害なしのため優先度低。

2. **2MB超カードの最適化（任意）**
   - 一部カードが 2MB を超える。フルサイズ JPEG 化や WebP 化で軽量化の余地。

3. **gen_mahakara_imagen4.py の整理**
   - 既に削除済。将来 Imagen 4 経由で再生成する場合は再作成。

## Generator への指示

なし（合格）。次の作業に進んで問題なし。
